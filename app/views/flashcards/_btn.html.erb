<% if messages.any? %>
  <%= button_to generate_flashcards_conversation_path(conversation),
          method: :post,
          class: "btn pera-btn-outline d-flex align-items-center justify-content-center gap-2 mb-5" do %>
    <i class="fa-solid fa-layer-group"></i> <%= t("flashcards.generate") %>
  <% end %>
<% end %>
